# Gastown-Boy Backend Configuration
# Copy this file to .env and customize as needed.
# All values shown are defaults - uncomment and modify to override.

# =============================================================================
# Server Configuration
# =============================================================================

# Port the API server listens on
# PORT=3001

# Node environment (development, production, test)
# NODE_ENV=development

# =============================================================================
# CORS Configuration
# =============================================================================

# Allowed origin for CORS requests (frontend URL)
# CORS_ORIGIN=http://localhost:5173

# =============================================================================
# Gastown Configuration
# =============================================================================

# The backend executes `gt` commands via child_process.spawn().
# GT_TOWN_ROOT is set automatically by `npm run dev` (defaults to ~/gt).
#
# Only set this if:
# - Running outside of `npm run dev`
# - Using a non-default gastown location
#
# Usage with npm run dev:
#   npm run dev                    # Uses ~/gt
#   npm run dev -- /path/to/town   # Uses custom path
#
# GT_TOWN_ROOT=~/gt

# =============================================================================
# Timeouts
# =============================================================================

# Default timeout for gt commands in milliseconds
# GT_COMMAND_TIMEOUT=30000

# =============================================================================
# ElevenLabs Voice Integration (T003)
# =============================================================================

# ElevenLabs API key for text-to-speech and speech-to-text
# Get your API key from: https://elevenlabs.io/app/settings/api-keys
# Required for voice features (playback, transcription, notifications)
# ELEVENLABS_API_KEY=your_api_key_here

# Audio cache directory for synthesized speech files
# Defaults to backend/.audio-cache if not set
# AUDIO_CACHE_DIR=.audio-cache

# Maximum age (in hours) before cached audio is eligible for cleanup
# AUDIO_CACHE_MAX_AGE_HOURS=168

# ElevenLabs model to use for TTS (eleven_turbo_v2 recommended for speed)
# Options: eleven_monolingual_v1, eleven_multilingual_v1, eleven_multilingual_v2, eleven_turbo_v2
# ELEVENLABS_MODEL=eleven_turbo_v2

# Audio output format
# Options: mp3_44100_128, mp3_44100_64, pcm_16000, pcm_22050, pcm_24000, pcm_44100
# ELEVENLABS_OUTPUT_FORMAT=mp3_44100_128

# Rate limit for ElevenLabs API (requests per minute)
# ELEVENLABS_RATE_LIMIT_RPM=100

# Maximum text length for single TTS request (characters)
# ELEVENLABS_MAX_TEXT_LENGTH=5000

# Voice config persistence path (defaults to ~/.gastown/voice-config.json)
# VOICE_CONFIG_PATH=~/.gastown/voice-config.json
