# Gastown-Boy Frontend Configuration
# Copy this file to .env and customize as needed.
# All values shown are defaults - uncomment and modify to override.

# =============================================================================
# API Configuration
# =============================================================================

# Backend API URL
# For local development, this defaults to localhost:3001
# For remote access (e.g., via ngrok), set this to your backend URL
# VITE_API_URL=http://localhost:3001

# =============================================================================
# Development Notes
# =============================================================================

# The Vite dev server runs on port 5173 by default and proxies /api requests
# to the backend. In production builds, VITE_API_URL must be set to the
# actual backend URL.
#
# For remote access:
#   1. Expose backend with ngrok: ngrok http 3001
#   2. Set VITE_API_URL to the ngrok URL
#   3. Rebuild or restart the frontend
#
# Example for ngrok:
# VITE_API_URL=https://abc123.ngrok.io
