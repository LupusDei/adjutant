# Adjutant Frontend Configuration
# Copy this file to .env and customize as needed.
# All values shown are defaults - uncomment and modify to override.

# =============================================================================
# Port Configuration
# =============================================================================

# Frontend dev server port (default: 4200)
# VITE_PORT=4200

# Backend API port for proxy (default: 4201)
# VITE_API_PORT=4201

# =============================================================================
# API Configuration
# =============================================================================

# Backend API URL (optional)
# By default, the frontend uses relative URLs (/api) which work with:
#   - Local development (Vite proxy forwards to backend)
#   - ngrok tunneling (just run `npm run tunnel`)
#
# Only set this if you need to hit a DIFFERENT backend (e.g., production):
# VITE_API_URL=https://api.example.com

# =============================================================================
# Remote Access (ngrok)
# =============================================================================
#
# For remote access from phone/other devices:
#   1. Start dev server: npm run dev
#   2. Start tunnel: npm run tunnel
#   3. Access via the ngrok URL
#
# No configuration needed! The Vite proxy handles API forwarding automatically.
#
# See README.md for full ngrok setup instructions.
